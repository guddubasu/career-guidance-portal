{
  "name": "career-guidance-portal",
  "version": "1.0.0",
  "type": "module",

  "scripts": {
    "backend": "cd backend && nodemon server.js",

    "ml": "cd backend && venv\\Scripts\\python app.py",

    "resume-ai": "cd \"backend/AI Resume Analyzer\" && venv\\Scripts\\python -m uvicorn app:app --reload --host 0.0.0.0 --port 8000",

    "frontend": "cd frontend && npm run dev",

    "start": "concurrently \"npm run backend\" \"npm run ml\" \"npm run resume-ai\" \"npm run frontend\"",

    "seed-colleges": "cd backend && node initialize_career_DB/initializeCollegeData.js"
  },

  "devDependencies": {
    "concurrently": "^9.0.0",
    "nodemon": "^3.0.0"
  }
}